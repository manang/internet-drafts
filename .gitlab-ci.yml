image: python:latest

build:
  - pip install xml2rfc

job 1:
  stage: build
  script: make;

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
