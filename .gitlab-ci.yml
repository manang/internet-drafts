image: martinthomson/i-d-template

stages:
  - build
  - deploy

internet drafts:
  stage: build
  script: 
    - make
    - mkdir public
    - cp *.txt *.html public
  artifacts:
    paths:
      - public
web page:
  stage: deploy
  script: 
    - mkdir public
    - cp -f *.txt *.html public
  artifacts:
    paths:
      - public
  only:
    - gh-pages