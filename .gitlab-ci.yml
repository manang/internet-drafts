image: martinthomson/i-d-template

stages:
  - build
  - deploy

internet drafts:
  stage: build
  script: 
    - make
    - mkdir public
    - cp *.txt *.html public
  artifacts:
    paths:
      - public
  only:
    - master
    
pages:
  stage: deploy
  script: 
    - cp -f page/*.html page/*.css  public
  artifacts:
    paths:
      - public
  only:
    - master